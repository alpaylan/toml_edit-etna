# marauders: name=inline_table_no_value_panic tags=[etna]
diff --git a/crates/toml_parser/src/parser/document.rs b/crates/toml_parser/src/parser/document.rs
--- a/crates/toml_parser/src/parser/document.rs
+++ b/crates/toml_parser/src/parser/document.rs
@@ -1236,17 +1236,7 @@
         })
         .map(|t| t.span())
         .unwrap_or_default();
-    match state {
-        State::NeedsKey => {}
-        State::NeedsEquals => {
-            receiver.key_val_sep(previous_span.after(), error);
-            receiver.scalar(previous_span.after(), Some(Encoding::LiteralString), error);
-        }
-        State::NeedsValue => {
-            receiver.scalar(previous_span.after(), Some(Encoding::LiteralString), error);
-        }
-        State::NeedsComma => {}
-    }
+    let _ = state;
     error.report_error(
         ParseError::new("unclosed inline table")
             .with_context(inline_table_open.span())
